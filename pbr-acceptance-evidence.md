# PBR 物理正确性 — 量化验收证据汇总

> 本文件汇总**所有已验证的量化结论**,以及每条结论的**复现命令**和**证据强度**。
> 分两部分:**离线验证**(本机随时可复现)与**运行期验证**(需要完整构建,当前受阻)。
>
> 配套计划:`pbr-exploration-plan.md`。一键复现:`pwsh tools/verify_all.ps1`(退出码 0 = 全过)。

---

## 1. 离线验证(已通过,可随时复现)

### 1.1 着色器编译 + GPU 结构体布局

**命令**:`pwsh tools/verify_shaders.ps1`

| 检查项 | 结果 |
|---|---|
| `ray_tracing.slang`(raygen / miss×2 / closesthit / anyhit) | 编译通过 |
| `rasterization.slang`(vertex + fragment) | 编译通过 |
| `blend_image.slang`(vertex + fragment) | 编译通过 |
| `energy_compensation.slang` 的 `float[32][16]` 静态常量表 | Slang 接受 |
| `material_data` 17 个成员偏移 | **逐字段一致** |
| `rt_scene_data` 15 个成员偏移 | **逐字段一致** |
| `model_data` / `light_data` 偏移 | 一致 |

编译选项与 `shader_compiler.cpp` 严格一致:`-target spirv -profile spirv_1_6
-matrix-layout-column-major -O3`,逐 entry point 声明 stage。

**为什么这条重要**:`scene_data.slang` 顶部注释警告过——`materials` 数组在 shader 里
按物理指针解引用,步长 = 结构体自然大小;任何偏移不一致都会让 `materials[i≥1]` 错位
→ 越界解引用 → **设备丢失**。这条校验把该风险从"靠人工核对"变成"机器比对"。

### 1.2 能量守恒(阶段 2 的核心)

**命令**:`python tools/pbr_verify_compensation.py`(报告:`tools/compensation_report.txt`)

| 检查 | 结果 |
|---|---|
| 白炉(F0=1,11 档粗糙度 × 4 档视角)能量守恒 | 最大误差 **0.0000** |
| 有色表面 F0 = 0.04 / 0.1 / 0.5 / 1.0 | 最大偏差 **0.00000** |
| 单散射 E_ss 自洽性(roughness→0 时为 1.0000、单调递减) | 通过 |
| 单散射固有能量损失(roughness=1, μ=1) | **38.6%**(文献量级 30%~45%) |

补偿结构:`k = (1-E_ss)/(E_ss + f_avg)`,由约束
`E_ss·f_avg·(1+k) + f_avg²·k = f_avg` 解出,已用穷举校验确认在任意
`(E_ss, f_avg)` 下精确成立。`E_ss` / `f_avg` 表由
`tools/pbr_gen_energy_table.py` 从解析恒等式 `E_ss = E_{L~VNDF}[G1(L)]` 生成
(不是拟合出来的经验系数)。

### 1.3 环境光照(阶段 3)

**命令**:`python tools/pbr_env_check.py`(报告:`tools/env_report.txt`)

| 检查 | 相对误差 |
|---|---|
| 恒定环境 `L≡1.0` → 辐照度 = π·L | **0.0000%** |
| 恒定环境 `L≡0.25` → 辐照度 = π·L | **0.0000%** |
| 方向性天空 `L=max(0,ny)` → 解析值 2/3 | **0.0077%** |
| 环境 NEE + BSDF 采样组合 vs 解析值 `albedo·L` | **0.0184%** |
| MIS 权重范围 | 恒在 (0,1) |

辐照度估计量的方差(方向性天空):1 样本 57%、**4 样本 29%**、16 样本 14%
→ `ENV_IRRADIANCE_SAMPLES = 4` 的选值依据。单帧噪声由时域累积压下去。

### 1.4 程序化测试几何

**命令**:`python tools/verify_sphere_mesh.py`

| 检查 | 结果 |
|---|---|
| 顶点严格落在球面 / 法线单位且同向 | 通过 |
| UV ∈ [0,1] | 通过 |
| 退化三角形 | **0** |
| 绕序向外 | **100%** |
| 每条边被 2 个三角形共享(封闭 2-流形) | 通过 |
| UV 手性与几何一致(TBN 前提) | 通过 |

### 1.5 EXR 参考对比工具

**命令**:`python tools/compare_exr.py --selftest`

覆盖四种已知差异情形(完全相同 / +1% 应通过 / +10% 应不通过 /
局部亮斑整体应通过但长尾应告警)。判据:平均亮度相对差 < 2%(整体能量),
绝对差长尾 `p99/mean`(区分"整体平移"与"局部物理错误")。

---

## 2. 构建与运行(已有实测数据)

**构建通过、程序可运行**,并已完成一轮机器化的运行期验证。

| 项 | 结果 |
|---|---|
| 完整构建 | **通过** |
| **khronos validation(Debug 版)** | 连续运行 45 秒 + 正常退出,**零错误、零警告**(stdout/stderr 全空) |
| 渲染图像良构性(800×600 EXR) | **alpha 恒为 1.000000**、非有限值 **0**、过曝 **0.0000%**、近黑 **0.0000%** |
| **帧率(Release,800×600,默认场景)** | **793 fps**(1.26 ms/帧) |
| **环境光开销** | 关闭环境光后 **1451 fps**(0.69 ms/帧)⟹ 环境光 **+0.57 ms/帧,占 45.3%** |
| 帧率(Debug,含 validation) | 790 ~ 1120 fps(不代表 Release) |
| 累积到收敛所需帧数 | **未单独测量** |

性能数据由 `tools/perf_probe.ps1` 采集:FPS 直接读窗口标题(应用每 0.5 s 更新一次),
环境光开关用热键 `J` 切换 —— 全程无需人工读数。

### 与改动前渲染的对比(同分辨率 800×600)

| 截图 | 亮度均值 | p01(最暗 1%) | max | 近黑占比 |
|---|---|---|---|---|
| `2026_07_14`(改动前) | 0.515 | **0.000** | 0.781 | 1.90% |
| `2026_08_31`(改动前) | 0.615 | **0.008** | 0.939 | 0.00% |
| **`2026_09_26`(改动后)** | 0.564 | **0.268** | 0.875 | 0.00% |

**解读**:整体曝光量级一致(0.564 vs 0.615,差约 8%),但**暗部被大幅抬高**。
这与"用真实天空辐照度替换恒定的 `AMBIENT_LIGHT = 0.25`"完全吻合 ——
旧的 0.25 经验项只给约 `albedo × 0.19` 的环境光,而真实 HDR 天空的辐照度
接近 `π·L`,环境贡献大约高 4~5 倍。**这是预期行为,不是 bug**,但视觉上会让画面明显更"平"。
若想回到旧观感,调小 `env_intensity` 即可(它是场景 UBO 的字段)。

> ⚠ 注意:旧截图不一定与当前截图机位/场景完全一致,故此对比是**指示性**而非严格回归。

### 新增的离线图像分析工具

`tools/image_stats.cpp` + `tools/build_image_stats.ps1` → `out/imgtools/image_stats.exe`。

为什么需要:项目在 C++ 侧链接了 OIIO,但**系统 Python 没有 OIIO 绑定**。
我一度手写了 Python EXR 解码器,但它**未通过"alpha 恒为 1"的自校验** ——
已删除(留一个会给出错误数据的工具比没有更危险),改为复用 OIIO。

### 白炉球体阵探针(全自动,已有首次实测)

`tools/furnace_probe.ps1` 把整条链路自动化:启动应用 → `PostMessage` 投递
`G`(球体阵)/`H`(白炉预设)/`C`(截图)→ 用 `image_stats.exe --dots` 在**每个球心**
取圆盘均值。

判据:`metallic = 1` 且反照率强制为白 ⟹ `F0 = 1` ⟹ 能量守恒要求
**亮度随 roughness 不变**。

**实测结果(2026_09_26_23_23_16.exr,半径 30 px 圆盘 = 球总反射功率的代理)**:

| metallic | r=0.05 | r=0.24 | r=0.43 | r=0.62 | r=0.81 | r=1.00 | 极差 |
|---|---|---|---|---|---|---|---|
| 0.00(介电) | 0.5031 | 0.4495 | 0.4516 | 0.4553 | 0.4491 | 0.4306 | 14.4% |
| 0.25 | 0.2948 | 0.3138 | 0.3345 | 0.3510 | 0.3726 | 0.3786 | 22.1% |
| 0.50 | 0.3474 | 0.3517 | 0.3556 | 0.3547 | 0.3562 | 0.3461 | 2.7% |
| 0.75 | 0.4452 | 0.4356 | 0.4208 | 0.4087 | 0.4052 | 0.3962 | 11.0% |
| 1.00 | 0.5271 | 0.5069 | 0.4894 | 0.4704 | 0.4543 | 0.4297 | **18.5%** |

**⚠ 这个结果不能用来判定能量守恒 —— 测试被环境不均匀性污染了。**

关键对照:`metallic = 0` 是介电,`F0 ≈ 0.04`,镜面分量只占约 4%,
单散射能量损失折算到总量只有约 1.5%;但它同样随粗糙度降了 **14.4%**。
既然介电行都降这么多,这个下降**不可能来自镜面能量损失** ——
主要来自"光滑球反射天空的特定方向、粗糙球平均整片天空"。

→ 结论:**非均匀天空做不出白炉**。这个测试**没有**给出能量守恒的像素级证据
(结论仍以 §1.2 的数值验证为准,那里误差是 0.0000)。

### 为此新增的白炉开关(待重建后复测)

要让白炉在像素级真正成立,必须让环境**均匀**、且**关掉直接光**:

| 位 | 名称 | 作用 |
|---|---|---|
| 12 | `uniform_environment` | 天空处处 = 1.0(辐照度解析值 = π) |
| 13 | `disable_direct_lights` | 关掉全部直接光,环境成为唯一光源 |

两者都已并入 `furnace_preset()`(即 `H` 热键与 UI"启动白炉预设"按钮)。
着色器是运行时从磁盘加载的,**无需重建即生效**;但 `furnace_preset()` 在 C++ 侧,
所以**按 `H` 需要重建一次**。

复测判据(重建后):30 个球的亮度应当**基本恒定**(在 OCIO 色调映射之后仍应平坦)。
若 `metallic=1` 行仍随粗糙度明显下降,才是真正的能量守恒问题。

### ✅ 真白炉(均匀环境 + 无直接光 + 关雾 + 白反照率)—— 已通过

在修掉上表第 18~21 条 bug、并加上"均匀环境/无直接光/关雾"三个开关与更大球间距之后:

| 量 | 值 |
|---|---|
| 30 个球的读数范围 | **0.6435 ~ 0.7069** |
| 背景(= 均匀天空 L=1 经同一色调映射) | **0.7065** |
| **最亮的球** | **0.7069 —— 与背景一致** ✓ |
| 通道一致性 | **R = G = B**(完全中性,符合均匀白环境) |
| 离散度 | **9.0%**(修 bug 前是 22%~30%) |
| 均值相对背景 | **−4.6%** |

**白炉判据是"白球在均匀天空下应当与背景同亮(隐形)"** —— 最亮的球精确满足,
其余球低 0~9%,且**通道完全中性**。这比修 bug 前的 22%~30% 离散度改善了一个量级。

**残差的来源(已排查)**:把 `MAX_BOUNCES` 从 8 提到 32,读数几乎逐位不变
(0.706936 → 0.706908),因此**不是多重弹射截断**;间距 0.85 下相邻球仍会遮挡
约几个百分点的半球入射,与观测到的 −4.6% 均值偏差量级相符。**残余是物理遮挡,不是 bug。**

> 一个仍然存在的测量瑕疵:探针的像素坐标由世界坐标投影算出,列间读数呈轻微
> 非对称(首列 0.7069 / 末列 0.6562),而球阵与光照在 x 上是对称的 ——
> 说明采样圆盘并未精确落在球心。这不影响"离散度从 30% 降到 9%"的结论,
> 但绝对值的几个百分点偏差里包含这部分误差。



> 注意:保存的 EXR 已经过 **OCIO 变换**(`scene_manager::save_image` 在写盘前做
> CPU OCIO),所以读数是**显示空间**而非线性 HDR。相对比较(平坦性)仍然有效,
> 但不能直接当反射率读。

### 这套运行期验证是怎么做的(可复现)

1. **validation**:跑 `out\Debug\chinese-chess.exe`(Debug 版启用 `VK_LAYER_KHRONOS_validation`,
   该层在 `HKLM\SOFTWARE\Khronos\Vulkan\ExplicitLayers` 中注册),stdout/stderr 重定向到文件;
   同时观察 `resources/cache/` 三个缓存文件是否在进程退出时被写回 —— 以此确认**析构真的跑完**。
2. **驱动 UI / 截图**:按键用 `PostMessage` 投递 `WM_KEYDOWN/WM_KEYUP`(**不抢焦点**,
   因此不干扰使用者)。截图键 `C` 由 `glfw_window::key_callback` 处理,
   写盘在后台 `jthread` 里异步进行。
3. **白炉场景**:热键 `G` 显示球体阵、`H` 切白炉预设、`J` 切环境光。
4. **读数**:`image_stats.exe <exr> [--blocks R C] [--dots x,y,...] [--dot-radius N]`。
   整套流程封装在 `tools/furnace_probe.ps1`。


---

## 3. 验证过程中被抓到并修掉的真实缺陷

这张表是本项目里"先建验证脚本"这个决定的实际回报 —— 下列问题**都很难靠读代码发现**:

| # | 缺陷 | 表现 | 抓到它的检验 |
|---|---|---|---|
| 1 | `sample_ggx_vndf` 的 Heitz 2018 实现多做两次变换 | 采样分布退化为近似均匀 | 密度分箱 |
| 2 | `NdotV` 在使用之后才声明 / `make_layer_bsdf` 少传参数 | 编译错误 | 离线 slangc 编译 |
| 3 | 补偿系数 `k` 的分母符号写反 | 有色表面能量随粗糙度下降 28% | 有色表面检查 |
| 4 | `F_avg` 表生成函数参数默认值写成 1.0 | 整张表恒为 1.0 | 表值抽查 |
| 5 | 环境 NEE 的"重试采样"概率补偿错误 | 估计量恰好是解析值的 **2 倍** | 辐照度/MIS 数值检验 |
| 6 | 环境 NEE 与 BSDF 采样命中天空**未做 MIS** | 环境能量翻倍 | 设计审查 + MIS 检验 |
| 7 | 程序化球体**绕序反向** | 面法线全朝内,背面剔除剔掉正面 | 球体拓扑校验 |
| 8 | 程序化球体**两极零面积三角形** | 浪费 BVH、极点拓扑不封闭 | 球体拓扑校验 |
| 9 | 二分量表面用颜色插值而非 BSDF 混合 | 刻字区"基底高光 + 刻字颜色" | 设计审查 |
| 10 | NEE 完全跳过透射材质 | 棋子失去直接光与高光 | 设计审查 |
| 11 | 程序化球体阵放在**相机背后**(z=+2.6,相机朝 -Z) | 完全看不见;且行沿 z 铺开会近排挡远排 | 运行期复查(几何/相机核对) |
| 12 | **介质状态机**:`entering_medium` 在 `inside_transmissive` 被清零之后才计算 → 恒为 true | 穿出介质后又被标记"在介质内",**空气路径**会被按下一跳物体的吸收系数衰减(两棋子前后重叠时中间空气被当玉石内部消光) | 运行期复查(状态机推演) |
| 13 | 全反射后介质状态未维护 | TIR 时光已不在原路径上,却继承了"已离开介质"的状态 | 运行期复查 |
| 14 | `prev_throughput` 只写不读 + 5 个调试字段常驻 ray payload | 浪费 ~50 字节/跳(`maxRayPayloadSize` 是实现相关上限) | 运行期复查(死字段扫描) |
| 15 | ORM 的 **AO 通道完全没生效**(`surf.occlusion` 只写不读) | 贴图 AO 无任何效果 | 运行期复查(读写扫描) |
| 16 | 天空索引守卫只查哨兵、不查 `< texture_count` | 越界索引会读未初始化 bindless 槽位(未启用 partially-bound) | 运行期复查(守卫一致性) |
| 17 | 5 个着色器函数是死代码 | — | 死代码扫描 |
| 18 | **能量补偿缩放系数写错**:`layer_specular_energy_scale` 用 `1 + f_avg·k`,应为 `1 + k` | 白炉(`f_avg=1`)恰好不受影响,但 F0=0.5 的金属欠 11%、介电镜面欠 35% —— **粗糙金属会比光滑金属暗** | **验证脚本的模型 vs 着色器实现交叉核对** |
| 19 | **环境 NEE 的 MIS 权重用了错的 pdf**:采样是上半球均匀 `1/(2π)`,权重却用余弦 `cosθ/π` | balance heuristic 两侧 pdf 不一致 → 权重错 | 阅读实现时对照采样函数 |
| 20 | **环境 NEE 不活跃时权重仍 < 1** | `disable_ambient` 只关掉 NEE 那一半,BSDF 路径仍被压低 → "关环境光"变成"环境光被系统性削弱"而不是归零 | `tools/dark_probe.ps1` 反证测试(白炉+关环境光,球不是纯黑) |
| 21 | **`sample_bsdf` 的 `out.pdf` 用单波瓣 pdf,而 `evaluate_bsdf` 用混合 pdf** | 估计量 `f·cos/p` 的分母必须是真实采样密度;用波瓣 pdf 会系统性低估间接光,且低估随 `p_ms`(∝roughness)增大 → **白炉球随粗糙度变暗** | 对照 `layer_pdf`(混合)与 `sample_bsdf`(单波瓣)的定义 |

### 关于第 18 条的重要说明(证据可信度)

第 18 条是靠"**验证脚本的模型** vs **着色器实现**"对照发现的。在此之前,
`tools/pbr_verify_compensation.py` 里写的是 `(1+k)`,而着色器里是 `(1+f_avg·k)` ——
两者只白炉等价。也就是说:**此前"能量误差 0.0000"验证的是脚本的公式,不是着色器的实现。**

现已修正:
- 着色器改为 `1 + k`(任意 F0 下总反射率精确等于 `f_avg`);
- 验证脚本里**写明了两侧的确切表达式**,并在注释里记录了这个教训。


> 另有若干**验证脚本自身**的错误(余弦采样器分量放错、常量环境期望值算错、
> 两条不同 pdf 的策略共用同一估计量形式、极点重复顶点误判为不封闭等)。
> 检验脚本出错会产生**假失败**,同样需要认真对待 —— 这点在计划文档里有记录。

---

## 4. 证据强度声明(诚实标注)

| 结论 | 强度 |
|---|---|
| 着色器可编译、GPU 布局一致 | **强**(编译器 + 反射机器比对) |
| 单散射 GGX 能量损失 38.6% | **强**(采样器自用 pdf 的解析恒等式,交替验证) |
| 多次散射补偿使能量守恒(数值) | **强**(数学恒等 + 穷举校验;脚本与着色器表达式已对齐) |
| 环境辐照度无偏、MIS 组合无偏 | **强**(解析期望 + 数值验证) |
| 测试几何拓扑正确 | **强**(完整流形校验) |
| 完整构建 + 程序可运行 | **强**(实测) |
| **GPU 侧无 validation 错误/警告** | **强**(Debug 版 + Khronos validation,45 秒零输出) |
| **渲染图像良构**(alpha=1、无 NaN、无过曝/全黑) | **强**(真实 EXR 的机器读数) |
| **白炉在像素级成立** | **中~强**:离散度 9%、最亮球与背景一致、通道中性;绝对值的几个百分点受残余遮挡与探针采样位置限制 |
| 帧率量级 | **强**(Release 实测;环境光开销 45.3%) |
| 与改动前观感一致性 | **弱**(指示性:机位/场景未必一致) |
| 累积收敛帧数 / GPU 时间 | **未测量** |

**已无"完全无证据"的项。** 仍缺的只是 GPU 时间与收敛帧数的精确记录(不影响正确性结论)。



