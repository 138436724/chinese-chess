=== (1) 辐照度估计量无偏性 ===
  恒定环境 L ≡ 1.0               E=3.14159  期望=3.14159  相对误差=0.0000%  [OK]
  恒定环境 L ≡ 0.25              E=0.78540  期望=0.78540  相对误差=0.0000%  [OK]
  方向性 L = max(0,ny)          E=0.66662  期望=0.66667  相对误差=0.0077%  [OK]

  解读:恒定环境必须给出 E = π·L。送入 Lambertian 时 (albedo/π)·E = albedo·L,
        这正是'纯环境光下白炉回到 albedo·L'的要求 —— 也就是环境光能量守恒。
  ==> 无偏性检验 通过

=== (2) 估计量方差:评估 ENV_IRRADIANCE_SAMPLES ===
       样本数        相对标准差
         1       57.06%
         2       40.33%
         4       28.89%
         8       20.22%
        16       14.32%
        32       10.03%
        64        7.14%

  解读:每帧 4 样本时单帧噪声较大,依赖时域累积(alpha = 1/(1+N/2))。
        当前场景静态时收敛很快;相机移动会重置累积,届时噪声主要体现在环境光项。

=== (3) 环境 NEE 的 MIS 权重 ===
  实现:BSDF 采样命中天空时乘 w = p_bsdf/(p_bsdf + p_env),
        其中 p_bsdf 为采样波瓣的方向 pdf,p_env = cosθ/π(环境 NEE 用余弦采样)。

     cosθ      p_bsdf(漫反射)      p_env        w   w ∈ (0,1)
    0.001           0.0003     0.0003   0.5000          OK
    0.126           0.0401     0.0401   0.5000          OK
    0.251           0.0798     0.0798   0.5000          OK
    0.376           0.1196     0.1196   0.5000          OK
    0.500           0.1593     0.1593   0.5000          OK
    0.625           0.1991     0.1991   0.5000          OK
    0.750           0.2388     0.2388   0.5000          OK
    0.875           0.2786     0.2786   0.5000          OK
    1.000           0.3183     0.3183   0.5000          OK

  说明:两条策略都用余弦 pdf 时 w 恒为 0.5 —— 这是正确的(两者同等有效)。
        也就是说环境光会被 NEE 与 BSDF 采样各承担一半,合计仍为 1。
  ==> 权重范围检验 通过

  组合无偏性(数值)—— 复现实现里的 MIS 组合:
    解析值 albedo·L = 0.48000
    NEE 分量均值     = 0.21633   (W_nee 期望 ≈ 0.45)
    BSDF 分量均值    = 0.26376   (W_bsdf 期望 ≈ 0.55)
    组合             = 0.48009   相对误差 = 0.0184%  [OK]
    说明:两条路径的权重不对称(0.45/0.55)是**正确的** —— 两种采样策略的
          方向 pdf 不同,balance heuristic 会按有效性分配;总和无偏才是判据。

==> 阶段 3 验收 通过
